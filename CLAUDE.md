# Claude Guidance

Use the same workflow guidance as `AGENTS.md`. This file exists so Claude-style agents can reuse the catalog without translating the GitHub Copilot customization layout.

Primary folders:

- `azure-devops-classic-to-yaml/`
- `arm-to-bicep-conversion/`
- `bicep-to-modules/`
- `secure-bicep-iac/`
- `finops-azure/`

When working in this catalog, reference the matching `.github/skills/<name>/SKILL.md` and `.github/agents/*.agent.md` files for the detailed procedure.

MCP is not allowed in this workspace. Use Azure CLI, Azure SDKs, REST APIs, exported files, and static documentation links for discovery and validation.
